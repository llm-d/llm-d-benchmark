version: 1
services:
  llm-d:
    image: llm-d:latest
    ports:
      - "8000:8000"
    environment:
      - "LLM_D_ENV=production"
    volumes:
      - "./data:/app/data"
    networks:
      - llm-d-network

networks:
  llm-d-network:
    driver: bridge