# [REQUIRED]
{% set base_dir = base_dir | default('../') -%}
base_dir: {{ base_dir }}

# [REQUIRED]
values_file:
  path: {{ base_dir }}/config/templates/values/defaults.yaml

# [REQUIRED]
template_dir:
  path: {{ base_dir }}/config/templates/jinja

# [OPTIONAL]
scenario_file:
  path: {{ base_dir }}/config/scenarios/guides/ferrite-7b.yaml

# Ferrite-vs-python comparison baseline at Qwen2.5-7B fp16.
# Profile sweep lives at workload/profiles/vllm-benchmark/ferrite_*.yaml.in
# (generated by scripts/generate-ferrite-matrix.py).
