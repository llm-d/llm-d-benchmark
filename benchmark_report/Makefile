# This is a minimal Makefile for code formatting.
# Once llm-d-benchmark is fully converted to Python, a single Makefile
# covering the repository will replace this.

# Format code
format:
	ruff format .
	black .
	ruff check --fix .